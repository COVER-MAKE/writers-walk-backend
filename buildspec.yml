version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17
  build:
    commands:
      - echo "Gradle 빌드 시작..."
      - chmod +x gradlew
      - ./gradlew clean build -x test
artifacts:
  files:
    - build/libs/*.jar
    - appspec.yml
    - scripts/**
  discard-paths: yes